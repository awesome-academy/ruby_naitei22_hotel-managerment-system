<p><%= t('devise.mailer.reset_password_instructions.hello', email: @resource.email) %></p>

<p><%= t('devise.mailer.reset_password_instructions.request_info') %></p>

<p><%= link_to t('devise.mailer.reset_password_instructions.link_text'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('devise.mailer.reset_password_instructions.ignore_info') %></p>
<p><%= t('devise.mailer.reset_password_instructions.password_info') %></p>
